import { useRef, useEffect, useState } from 'react';
import cn from 'classnames';
import styles from './MainContainer.module.scss';

const Main = () => {
  return <div className={cn(styles.mainRoot)}>hello</div>;
};

export default Main;
